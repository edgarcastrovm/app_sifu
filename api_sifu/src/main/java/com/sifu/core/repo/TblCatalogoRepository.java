package com.sifu.core.repo;

import com.sifu.core.utils.entity.TblCatalogo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TblCatalogoRepository extends JpaRepository<TblCatalogo, Integer> {
}